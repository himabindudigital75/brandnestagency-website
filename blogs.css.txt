/* BLOGS PAGE EXTRA CSS */

.blogs-hero {
  padding-top: 150px;
}

.blog-grid {
  margin-top: 60px;
  display: grid;
  grid-template-columns: repeat(auto-fit,minmax(300px,1fr));
  gap: 30px;
}

.blog-card {
  background: #fff;
  border-radius: 18px;
  box-shadow: 0 12px 25px rgba(0,0,0,0.1);
  padding: 20px;
  transition: .3s;
}

.blog-card:hover {
  transform: scale(1.03);
}
